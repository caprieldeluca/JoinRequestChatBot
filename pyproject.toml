[project]
name = "joinrequestchatbot"
version = "0.1.0"
description = "Telegram bot to handle join request in an anonymized chat."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-telegram-bot[job-queue]>=20.1",
    "pyyaml>=6.0.3",
]
